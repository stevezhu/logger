{
  "name": "@t/logger",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  }
}
