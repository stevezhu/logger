{
  "name": "@t/logger",
  "version": "0.0.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && tsc -p tsconfig.browser.json",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/stream-buffers": "^3.0.3",
    "jest": "^24.9.0",
    "stream-buffers": "^3.0.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.6.2"
  }
}
